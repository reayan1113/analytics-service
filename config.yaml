server:
  host: "0.0.0.0"
  port: 8000

# Source database (read-only) - contains orders and order_items
order_database:
  host: "fullstack-mysql-server.mysql.database.azure.com"
  port: 3306
  username: "nidula"
  password: "springboot@inteli020"
  database: "orderdb"

# Analytics database (read-write) - contains analytics tables
analytics_database:
  host: "fullstack-mysql-server.mysql.database.azure.com"
  port: 3306
  username: "nidula"
  password: "springboot@inteli020"
  database: "analyticsdb"

forecasting:
  moving_average_window: 7
  enable_linear_regression: true
  enable_ensemble_method: true
  exponential_smoothing_alpha: 0.3
  trend_smoothing_beta: 0.1
  seasonal_periods: 7
  polynomial_degree: 2
  outlier_detection_enabled: true
  # Historical windows for forecasting
  history_days_daily: 30
  history_days_hourly: 7

scheduler:
  enabled: true
  run_time: "00:00"
